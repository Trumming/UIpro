#include "stdafx.h"

/*
 *@brief 
 *@param
 *@retval 
 *@note

static unsigned char PharseCMD(Queue *pQueue)
{
	unsigned char c_start = QueueExtract(pQueue);
	unsigned char cmd[3] = {0};
	unsigned char arg[2] = {0};
	if('$' == c_start)
	{
		if(RET_SUCCESS == QueueExtractEx(pQueue,cmd,sizeof(cmd)))
		{

		}
		else
		{

		}
	}

}*/